dependencies {
    compileOnly project(":dtf-core")
    annotationProcessor project(":dtf-core")
}

compileJava{
    doFirst{
        options.compilerArgs = [
                "-g","-verbose"
        ]
    }
}
